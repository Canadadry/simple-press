# Todo

 - add in navbar a backup button that download currend sqlite db saved
 - add in navbar a restore button that upload adn replace currend sqlite db
 - add a deploy button that run a script (possibly a cp from the admin storage to the prod or  with rclone or whatever)
